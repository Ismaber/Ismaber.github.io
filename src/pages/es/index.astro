---
import Page from '../../templates/Home.astro';
---

<!--
  This file simply reuses the generic index page.  When Astro's
  internationalization router sees a page in the "es" directory, it
  will assign the locale accordingly.  By delegating to the shared
  page, you avoid duplicating markup for each language.
-->

<Page />